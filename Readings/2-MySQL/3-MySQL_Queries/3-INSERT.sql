		-- we are using twitter database as example
            -- here we use INSERT INTO(to insert) then we specify *table name* as well as their "parameters" like so (column_name, column_name, etc) 
				-- now, we can add someting like VALUES to add a new output like so ("example text", idnum, datetime(NOW(), datetime(NOW());
					-- example below
-- INSERT INTO tweets (tweet, user_id, created_at, updated_at) VALUES ("hey wassup", 1, NOW(), NOW());
                    -- run it
			-- if successful we can now run

SELECT * FROM tweets;
-- now it has been inserted into the database.